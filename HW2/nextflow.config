params {
    reads        = null
    sra_ids      = null
    reference    = null
    outdir       = "results"
    paired = false
}

process {
    executor = 'local'
}

timeline {
    enabled = true
}

process {
    conda = "environment.yml"
}
